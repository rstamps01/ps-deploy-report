# VAST As-Built Report Generator - Development Dependencies
# These are NOT bundled into the self-contained application.

# Include runtime deps
-r requirements.txt

# PDF generation (optional fallback, requires system C libraries)
weasyprint>=60.0
cairocffi>=1.7.0
pycairo>=1.28.0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Code quality and linting
flake8>=6.0.0
black>=23.0.0

# Packaging
pyinstaller>=6.0.0
