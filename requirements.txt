# VAST As-Built Report Generator - Runtime Dependencies
# These are the packages bundled into the self-contained application.

# Core HTTP and API libraries
requests>=2.33.0
urllib3>=2.6.3

# Configuration management
PyYAML>=6.0.1

# PDF generation (primary engine)
reportlab>=4.0.0

# Dynamic diagram generation (PNG conversion)
rl-renderPM>=4.0.3
# Windows PDF-to-PNG fallback when renderPM fails (T1 font); also useful on Linux
pymupdf>=1.24.0

# Image processing for diagram sizing
Pillow>=12.1.1

# SVG diagram generation (rack-centric network topology)
svgwrite>=1.4.0
cairosvg>=2.7.0

# CLI argument parsing
click>=8.1.0

# Logging enhancements
colorlog>=6.7.0

# Date/time handling
python-dateutil>=2.8.0

# JSON schema validation
jsonschema>=4.19.0

# Environment variable management
python-dotenv>=1.0.0

# Web UI (Flask-based GUI)
flask>=3.0.0

# Markdown rendering (in-app documentation viewer)
markdown>=3.5.0

# Cross-platform SSH (used for port mapping on all platforms)
paramiko>=3.4.0

# Interactive SSH for Mellanox Onyx switches (macOS/Linux only)
pexpect>=4.9.0; sys_platform != "win32"
scp>=0.15.0
