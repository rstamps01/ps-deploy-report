Browser console messages captured during the TP-1 reproduction (Reporter / Run Discovery
against 192.168.2.2 with Tech Port mode on, custom credentials, Autofill off).

No JavaScript errors or warnings were raised by the Reporter UI itself during the
failure. The four entries below are MCP-instrumentation noise:

  - "Native dialog overrides installed - dialogs are now non-blocking"
    (CursorBrowser MCP banner; emitted twice on page load)
  - "Dialog suppressed: prompt - Profile name (e.g. \"selab-var-202\"): (returning: )"
    (emitted when openPlacementModal()/Save-to-Profile flow tried to open a
     window.prompt; suppressed by the MCP. Unrelated to the /api/discover failure.)

That the UI logged ZERO errors is itself a finding for TP-1: the only signal a user
sees on the page is the modal text "Error: Could not parse management IP from ip addr
output: ''" (see 02-post-click-error.png). There is no console diagnostic and no
toast/banner outside the modal -- a user without server-log access has nothing to
take to support beyond the cryptic message.

----- raw messages -----

[1] 2026-04-29T20:46:31Z  warning
    "[CursorBrowser] Native dialog overrides installed - dialogs are now non-blocking"

[2] 2026-04-29T20:46:31Z  warning
    "[CursorBrowser] Native dialog overrides installed - dialogs are now non-blocking"

[3] 2026-04-29T20:48:31Z  warning
    "[CursorBrowser] Dialog suppressed: prompt - Profile name (e.g. \"selab-var-202\"): (returning: )"

[4] 2026-04-29T20:48:32Z  warning
    "[CursorBrowser] Dialog suppressed: prompt - Profile name (e.g. \"selab-var-202\"): (returning: )"
