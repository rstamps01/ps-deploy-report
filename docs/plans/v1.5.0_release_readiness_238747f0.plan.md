---
name: v1.5.0 Release Readiness
overview: Comprehensive assessment of all items needed before tagging v1.5.0, including version consistency fixes, documentation updates across 15+ files, configuration template refresh, CI alignment, uncommitted work, and feature completion decisions.
todos:
  - id: p0-version-fix
    content: "P0: Fix src/__init__.py docstring version (1.4.7 -> 1.5.0) and verify check-version-sync.sh passes"
    status: completed
  - id: p0-beta-badge
    content: "P0: Remove Beta badge from base.html and app.css (REL-1); update README footer"
    status: completed
  - id: p0-commit-all
    content: "P0: Stage all 57 modified files + 9 untracked files and create organized commits"
    status: completed
  - id: p0-quality-gate
    content: "P0: Run full quality gate (flake8, black, mypy, pytest with coverage) and fix all failures"
    status: completed
  - id: p0-ci-threshold
    content: "P0: Fix build-release.yml cov-fail-under from 47 to 60 to match ci.yml and pyproject.toml"
    status: completed
  - id: p1-changelog
    content: "P1: Update CHANGELOG.md 1.5.0 section with all missing changes (Reporter, hamburger menu, One-Shot overhaul, VAST logo progress, badges, connection labels, manual switch placement, validation results, library improvements)"
    status: completed
  - id: p1-readme
    content: "P1: Update README.md — page table, CLI options, features, coverage threshold, project structure, Reporter page docs"
    status: completed
  - id: p1-deployment-docs
    content: "P1: Update v1.4.x references in INSTALLATION-GUIDE.md (6 refs), WINDOWS-ZIP.md, BUILD-STATUS.md, CI-VALIDATION-REPORT.md, RELEASE-WORKFLOW-LOG-REVIEW.md"
    status: completed
  - id: p1-todo-roadmap
    content: "P1: Update TODO-ROADMAP.md — coverage threshold to 60, mark REL-1 done, refresh next steps, update Last updated date"
    status: completed
  - id: p1-ops-docs
    content: "P1: Update ADVANCED-OPERATIONS.md and POST-INSTALL-VALIDATION.md — Reporter page, One-Shot overhaul, vperfsanity step count fix, SSH proxy hop"
    status: completed
  - id: p2-config-template
    content: "P2: Update config/config.yaml.template to reflect all new settings and features since last update"
    status: completed
  - id: p2-pyinstaller-spec
    content: "P2: Verify PyInstaller spec includes all new assets; add ADVANCED-OPERATIONS.md and POST-INSTALL-VALIDATION.md to bundle datas"
    status: completed
  - id: p2-doc-registry
    content: "P2: Add ADVANCED-OPERATIONS.md and POST-INSTALL-VALIDATION.md to _DOC_REGISTRY in app.py for in-app docs viewer"
    status: completed
  - id: p3-decisions
    content: "P3: Decide scope for NET-1 (nb_eth_mtu), AO-15, AO-19 — ship in v1.5.0 or defer"
    status: completed
  - id: p4-release-notes
    content: "P4: Create RELEASE_NOTES_v1.5.0.md with executive summary of this major release"
    status: completed
  - id: p4-stash-review
    content: "P4: Review 2 git stashes for any needed work before release"
    status: completed
  - id: p4-security-scan
    content: "P4: Run pip-audit and bandit locally to pre-empt CI security scan findings"
    status: completed
  - id: p4-merge-strategy
    content: "P4: Plan merge path: feature branch -> develop -> main -> tag v1.5.0"
    status: completed
isProject: false
---

# v1.5.0 Release Readiness Plan

## Current State Summary

- **Branch**: `feature/health-check-v2` — 6 commits ahead of remote, 57 modified files + 9 untracked files in working tree
- **Version strings**: `APP_VERSION`, `__version__`, `main.py --version`, `vast-reporter.spec` all set to `1.5.0`
- **Critical inconsistency**: `src/__init__.py` docstring line 7 says `Version: 1.4.7` while `__version__` is `1.5.0` — this will **fail `check-version-sync.sh`** in CI
- **2 git stashes** exist (`feature/health-check` WIP, `main` WIP) — review if either contains needed work
- **Coverage**: ~60% (`pyproject.toml` and `ci.yml` enforce 60, but `build-release.yml` still says 47)

---

## P0 — Blockers (must fix before tagging v1.5.0)

### 1. Fix version string inconsistency

- [src/**init**.py](src/__init__.py) line 7: change `Version: 1.4.7` to `Version: 1.5.0`
- `scripts/check-version-sync.sh` compares this docstring to `__version`__ — CI will reject the build until fixed

### 2. Remove Beta badge (REL-1)

- [frontend/templates/base.html](frontend/templates/base.html): remove the `<span class="nav-badge-beta">Beta</span>` element
- [frontend/static/css/app.css](frontend/static/css/app.css): remove `.nav-badge-beta` CSS class
- [README.md](README.md) footer: change `1.5.0 (Beta)` to `1.5.0`

### 3. Stage, commit, and push all working-tree changes

- 57 modified tracked files need staging
- 9 untracked files need `git add`:
  - `frontend/static/img/vast-icon-mask.png`
  - `frontend/templates/validation_results.html`
  - `src/result_scanner.py`
  - `src/utils/ops_log_manager.py`
  - `tests/test_oneshot_runner.py`
  - `tests/test_ops_log_manager.py`
  - `tests/test_result_scanner.py`
  - `tests/test_external_port_mapper.py`
  - `tests/test_tool_manager.py`
- Exclude: `logs/`, `output/`, `.DS_Store`, `docs/superpowers/` (generated/runtime artifacts)

### 4. Run full quality gate and fix failures

- `flake8 src/ tests/`
- `black --check --line-length 120 src/ tests/`
- `mypy src/ --ignore-missing-imports --no-strict-optional`
- `python -m pytest tests/ -v --ignore=tests/test_ui.py --ignore=tests/test_integration.py --cov=src --cov-fail-under=60`

### 5. Fix CI coverage threshold inconsistency

- [.github/workflows/build-release.yml](.github/workflows/build-release.yml) line ~91: change `--cov-fail-under=47` to `--cov-fail-under=60` to match `ci.yml` and `pyproject.toml`

---

## P1 — Documentation Updates (should complete before release)

### 6. Update CHANGELOG.md with missing recent changes

The `## [1.5.0]` section is missing many changes implemented since it was last updated. Add entries for:

- **Reporter page** — new `/reporter` page replacing Advanced Ops as primary user-facing workflow
- **Navigation restructuring** — hamburger menu for legacy/developer pages; Dashboard, Reporter, Results, Library, Docs as primary nav
- **One-Shot UI overhaul** — Pre-Validation converted to checkbox, "Run" button rename, operation badges (Net Test, Sys Test, Perf Test, Pull Logs, Pull Config), Reporter/One-Shot toggle styling
- **VAST Logo progress indicator** — CSS mask-image fill animation with stopwatch timer and percentage
- **Validation Results enhancements** — tab improvements, output directory tile, cluster profile dropdown, SVG icons, icon action buttons
- **Library page improvements** — device image info tooltip, styled upload button, removed helper text
- **Connection Settings labels** — renamed to "Cluster IP / VMS", "VMS Username", "VMS Password" with info tooltips
- **Manual switch placement** — UI workflow for manually adding switches when discovery fails
- **Rack diagram** — removed placeholder Arista fallback; manual placement data now correctly flows to report
- **Dynamic log tier filtering** — fixed log_tier passthrough on Reporter and Advanced Ops output panes
- Update release date from `2026-03-24` to actual release date
- Remove `(Beta)` label from version header

### 7. Update README.md

- **Page table**: Add "Reporter" page; update "Results" (renamed from "Validation Results"); note hamburger menu for Legacy/Developer pages
- **CLI options table**: Add `--no-proxy-jump`
- **Key features table**: Mention Reporter page, VAST logo progress, SSH proxy hop
- **Coverage threshold**: Change from "55%" to "60%" in Development section
- **Project structure**: Add `reporter.html`, `validation_results.html`, `result_scanner.py`, `ops_log_manager.py`, `oneshot_runner.py`
- **Advanced Operations section**: Mention Reporter page as the standard-mode interface

### 8. Update deployment docs with v1.5.0 references

- [docs/deployment/INSTALLATION-GUIDE.md](docs/deployment/INSTALLATION-GUIDE.md): 6 references to `v1.4.7` on lines 23, 28, 48, 52, 100, 304 — change all to `v1.5.0`
- [docs/deployment/WINDOWS-ZIP-PACKAGE-AND-WORKFLOWS.md](docs/deployment/WINDOWS-ZIP-PACKAGE-AND-WORKFLOWS.md): references `v1.4.2` — update examples
- [docs/BUILD-STATUS.md](docs/BUILD-STATUS.md): references `v1.4.1`/`v1.4.2` — refresh or mark as historical
- [docs/CI-VALIDATION-REPORT.md](docs/CI-VALIDATION-REPORT.md): references `v1.4.1` — refresh or mark as historical
- [docs/RELEASE-WORKFLOW-LOG-REVIEW.md](docs/RELEASE-WORKFLOW-LOG-REVIEW.md): references `v1.4.2` — refresh or mark as historical

### 9. Update TODO-ROADMAP.md for release

- Coverage threshold: change "cov-fail-under=55" to "cov-fail-under=60"
- Move completed items to Done section (PM-1 already marked done)
- Mark REL-1 as Done after Beta badge removal
- Update "Last updated" date
- Update "Next steps" to reflect post-1.5.0 focus
- Decide status of NET-1 (nb_eth_mtu): ship in 1.5.0 or defer to 1.5.1?

### 10. Update ADVANCED-OPERATIONS.md

- Document Reporter page as the standard-mode interface for workflows
- Document One-Shot UI overhaul (badge system, Pre-Validation checkbox, progress indicator)
- Document SSH proxy hop toggle behavior
- Fix vperfsanity step count if inconsistent (POST-INSTALL-VALIDATION.md says 6 steps, ADVANCED-OPERATIONS.md says 7)

### 11. Update POST-INSTALL-VALIDATION.md

- Fix vperfsanity step count (should be 7 per changelog and code)
- Add SSH proxy hop as a configuration consideration

---

## P2 — Configuration and Technical Alignment

### 12. Update config/config.yaml.template (NEW TODO ITEM)

The configuration template has not been updated to reflect features added since it was last modified. Changes needed:

- **ops_log settings**: Already present in template but missing from local `config.yaml` — ensure parity
- **proxy_jump**: Add `ssh.proxy_jump: true` default or document that this is a runtime/profile setting
- **reporter_defaults**: Consider adding Reporter page default settings section
- **Document all new config keys** in README Configuration section
- Review whether any new runtime behaviors should be config-driven vs. hard-coded

### 13. PyInstaller spec — verify bundle completeness

- [packaging/vast-reporter.spec](packaging/vast-reporter.spec): Confirm `frontend/templates/` wildcard copy picks up `reporter.html` and `validation_results.html` (it uses directory copy, so it should)
- Confirm `frontend/static/img/vast-icon-mask.png` is included (directory copy should cover it)
- **Missing from bundle**: `docs/ADVANCED-OPERATIONS.md` and `docs/POST-INSTALL-VALIDATION.md` are NOT in the `datas` list — add them if the in-app Docs viewer should serve them

### 14. Add ADVANCED-OPERATIONS.md and POST-INSTALL-VALIDATION.md to in-app docs

- [src/app.py](src/app.py) `_DOC_REGISTRY` (line 40): Currently lists 10 docs but does not include `ADVANCED-OPERATIONS.md` or `POST-INSTALL-VALIDATION.md`
- Add entries for both docs
- Add corresponding `datas` entries in `vast-reporter.spec`

---

## P3 — Feature Decisions for v1.5.0

### 15. NET-1: nb_eth_mtu implementation

- Flagged for v1.5.0 in TODO-ROADMAP
- Scope: collect `nb_eth_mtu` from `/api/v7/vms/1/network_settings/`, add to dataclass, extractor, PDF table, JSON output
- **Decision needed**: Ship with v1.5.0 or defer to v1.5.1 patch?

### 16. In-progress items (AO-15, AO-19): scope decision

- AO-15 (Advanced Ops hardening) and AO-19 (dynamic log levels, state persistence) are both "In progress"
- **Decision needed**: Are these complete enough for release, or should they be marked as partially complete / deferred items?

---

## P4 — Considerations You May Be Missing

### 17. Confluence page refresh

- Per `documentation-08.mdc` rule: "Local copies stored in `docs/confluence/` should be refreshed from Confluence before major releases"
- The 26 confluence docs reference "v1.3.0" in their titles — consider updating the Confluence page (ID: 6664028496) to reflect v1.5.0

### 18. RELEASE_NOTES_v1.5.0.md

- Per release workflow rules, optionally create a dedicated release notes file
- Given the massive scope of changes (Reporter page, One-Shot overhaul, SSH proxy hop, health check integration, advanced ops, 100+ UI improvements), a dedicated release notes document would be valuable

### 19. Git stashes

- `stash@{0}`: "WIP before health-check-v2 port" on `feature/health-check`
- `stash@{1}`: "WIP: library + pywebview combined changes" on `main`
- Review both to ensure no needed work is trapped in stashes

### 20. Branch merge strategy

- Currently on `feature/health-check-v2` — need to merge to `develop`, then `develop` to `main`, then tag
- The feature branch has 57 modified + 9 untracked files that are NOT yet committed (only 6 commits are ahead of remote)
- This means the vast majority of v1.5.0 work exists only in the working tree — **a single commit or series of commits is urgently needed**

### 21. Security scan

- `security.yml` runs on `requirements*.txt` changes — `requirements.txt` is modified, so this will trigger
- Run `pip-audit` and `bandit` locally before release to pre-empt CI findings

### 22. Reporter page not gated by developer mode

- The Reporter page is in standard navigation but depends on `/advanced-ops/`* API endpoints
- Verify the route guard fix (exact path matching vs prefix matching) still works correctly after all UI changes

### 23. Docs page developer-mode gating

- Per previous conversation, the Live API Explorer on the Docs page should only be visible in developer mode — verify this restriction is in place

### 24. Import PDF review

- `import/latest-report-missing-sections.pdf` is open in the IDE — this suggests there may be known report generation issues to address before release

---

## Priority Summary

| Priority                | Items                                                                     | Action                       |
| ----------------------- | ------------------------------------------------------------------------- | ---------------------------- |
| **P0 - Blockers**       | Version fix, Beta badge, commit all work, quality gate, CI threshold      | Must complete before tag     |
| **P1 - Documentation**  | CHANGELOG, README, deployment docs, TODO-ROADMAP, ops docs                | Should complete before tag   |
| **P2 - Technical**      | Config template, PyInstaller spec, in-app docs registry                   | Should complete before tag   |
| **P3 - Features**       | NET-1, AO-15/AO-19 scope decisions                                        | Decide: ship or defer        |
| **P4 - Considerations** | Confluence refresh, release notes, stashes, merge strategy, security scan | Review and address as needed |
